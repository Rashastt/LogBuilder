[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "logcatcher"
version = "1.1.0"
description = "Logging module!"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Vinicius" }
]